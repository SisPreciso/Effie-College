<div id="qst5a" class="tabcontent" style="display:block">    <div class="fila">        <div class="columna columna-1">            <label>{{ $qst5a->code }}</label>            <ul>                <li>                    La duración máxima del video que subas es de 3 minutos. No te pases de ese tiempo.                </li>                <li>                    Debes subir el video en formato 1080 px, comprimido en H.264 (es recomendable que sea mp4) o también                    realizar el envío por Quicktime, en alta resolución y comprimido en H.264.                </li>                <li>                    Nombrar tu archivo como: <b>Marca_IDequipo(video).mp4</b>                </li>                <li>                    El <b>Formulario de Autorización, Confidencialidad y Cesión de derechos</b> (<a                        href="documentos/2022_EffieCollegePeru_Formulario Autorización.docx" alt="Descargar documento"                        target="_blank" style="color: #cbb27c;font-style: italic;">descárgalo aquí</a>) acredita que ni                    el tutor académico ni los alumnos tienen vínculo alguno con la marca, su competencia y/o alguna de                    sus agencias. Este formulario, además, explicita que el tutor académico labora en la institución                    donde se forman los alumnos miembros del equipo. Súbelo en formato PDF, con un tamaño menor a 200KB.                    Nómbralo <b>Marca_IDequipo(firma).pdf</b>                </li>            </ul>            <label>{{ $qst5a->remark }}</label>            @if(!$user->is_completed)                <form method="POST" action="{{ route('files.store') }}" role="form" id="frm-file" class="dato">                    @csrf                    <div class="files">                        <div class="columna columna-2">                            <input type="file" name="filedata" id="filedata" required>                        </div>                        <div class="columna columna-3">                            <input type="text" name="filename" id="filename" maxlength="100"                                   placeholder="Nombre del archivo" required>                        </div>                        <div class="columna columna-4">                            <center>                                <button type="submit" class="btn-effie">{{ __('Agregar') }}</button>                            </center>                        </div>                    </div>                    <div class="span-done" id="ans5a-done-div"><span id="ans5a-done-msg"></span></div>                    <div class="span-fail" id="ans5a-fail-div"><span id="ans5a-fail-msg"></span></div>                </form>            @endif            <h3 class="card-subtitle">{{ __('Archivos cargados') }}</h3>            <table id="tbl-files" class="tablealumno">                <thead>                <th width="40%">{{ __('Archivo') }}</th>                <th width="40%">{{ __('Nombre (alias)') }}</th>                <th width="10%">{{ __('Fecha y hora de carga') }}</th>                @if (!$user->is_completed)                    <th width="10%">{{ __('Eliminar') }}</th>                @endif                </thead>                <tbody>                @foreach ($user->files as $file)                    <tr>                        <td><i class="fa fa-file-o"></i>&nbsp;{{ $file->filedata }}</td>                        <td>{{ $file->filename }}</td>                        <td>                            <center>{{ Carbon\Carbon::parse($file->created_at)->format('d/m/Y H:i') }}</center>                        </td>                        @if (!$user->is_completed)                            <td>                                <center><a onclick="removeFile({{ $file->id }})"><i class="fa fa-trash"></i></a>                                </center>                            </td>                        @endif                    </tr>                @endforeach                </tbody>            </table>            @if($user->is_completed)                <h3 class="card-subtitle">{{ __('Video cargado') }}</h3>                @php($video = '')                @foreach($user->files as $file)                    @if(Illuminate\Support\Facades\File::extension($file->filedata) === 'mp4')                        @php($video = $file->filedata)                    @endif                @endforeach                <center>                    @if($video)                        <video width="600" controls>                            <source src="{{ '../storage/app/'.$video }}" type="video/mp4">                            {{ __('Your browser does not support the video tag.') }}                        </video>                    @else                        <div class="text-inscripcion">                            <p>{{ __('El equipo no ha enviado video.') }}</p>                        </div>                    @endif                </center>                <div class="space"></div>            @else                <form method="POST" action="{{ route('sendProposal') }}" role="form" id="frm-send">                    @csrf                    <center>                        <button type="submit"                                onclick="return confirm('Estás a punto de enviar tu propuesta para la marca elegida por tu equipo. Pasado este punto, no podrás editar tus respuestas. ¿Deseas continuar?')"                                class="btn-effie">{{ __('Enviar') }}</button>                    </center>                </form>            @endif            <div class="space"></div>        </div>    </div></div>